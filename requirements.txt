# ── BIGTECH ALPHA — Python dependencies ──────────────────────────────────────
# Install: pip install -r requirements.txt

# Core app framework
streamlit>=1.35.0

# Data handling
pandas>=2.2.0
numpy>=1.26.0

# Visualisation
plotly>=5.22.0

# Scientific / statistics
scipy>=1.13.0

# Live market data
yfinance>=0.2.61

# Timezone handling
pytz>=2024.1

# HTTP (used internally by yfinance & streamlit)
requests>=2.32.0

# Optional: auto-refresh in Live Dashboard page
streamlit-autorefresh>=1.0.1

# ── ML / Forecasting ─────────────────────────────────────────────────────────

# Classical ML — regression, classification, clustering, pipelines
scikit-learn>=1.5.0

# Time-series econometrics — ARIMA, SARIMAX, ETS, VAR, OLS
statsmodels>=0.14.2

# Facebook/Meta Prophet — trend + seasonality forecasting
prophet>=1.1.5

# Gradient boosting — fast tree-based models (great for tabular finance data)
xgboost>=2.0.3

# Lightweight gradient boosting — low-dependency alternative to XGBoost
lightgbm>=4.3.0

# Feature engineering & selection helpers
feature-engine>=1.8.0

# Model explainability — SHAP values for any ML model
shap>=0.45.0
